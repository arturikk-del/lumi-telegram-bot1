services:
  - type: web
    name: lumi-bot
    env: python
    region: frankfurt
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: YANDEX_API_KEY
        sync: false
      - key: CATALOG_ID
        sync: false
      - key: TELEGRAM_TOKEN
        sync: false
